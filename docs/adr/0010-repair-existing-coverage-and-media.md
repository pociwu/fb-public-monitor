# 修復既有覆蓋狀態並只保留最高解析度媒體

部署時先做一致性備份，把沒有明確終點證據的舊 `backfill_done` 改為未驗證或來源受限，並將只由登入 Chromium 得出的公開狀態降為「監控帳號可瀏覽」。Additive migration 只建立別名與待修復清單，不刪除或合併實體檔案；V2 特殊帳號 canary 通過後才執行修復。重複貼文依標準身分合併；媒體只有在來源身分、SHA-256，或同內容範圍內的 pHash、比例與裁切條件共同證實相同時才合併，所有引用原子改指最高解析度並驗證完成後立即刪除低解析度檔案，不保留隔離副本。
