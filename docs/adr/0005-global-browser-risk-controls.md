# 所有 Chromium 工作共用全域風控限制

自動與手動 Chromium 工作均採單工執行，同帳號批次間隔 30–60 分鐘、跨帳號間隔 2–5 分鐘；每批最多兩篇貼文、20 次相簿操作或三分鐘，每帳號每日最多八批。匿名與登入瀏覽器共用同一個 OCI/IP 風控閘門；checkpoint、驗證碼、HTTP 429 或封鎖會觸發全域 24 小時熔斷，七天內重複發生則延長至 72 小時。單純登入狀態失效或匿名登入牆只記錄為無法判定並要求重新登入，不誤判為平台封鎖；手動按鈕只能提高優先級而不能繞過限制。
